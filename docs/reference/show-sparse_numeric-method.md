# Display a sparse_numeric object

`show` method for `sparse_numeric` prints the length, number of non-zero
entries, and the first few non-zero positions/values.

## Usage

``` r
# S4 method for class 'sparse_numeric'
show(object)
```

## Arguments

- object:

  A `sparse_numeric` object.
